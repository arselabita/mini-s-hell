shows nice history 
git log --all --graph --oneline


switch branch
git checkout <branchname>

Jump to commits:
git checkout <commit ID>

Jump back to main:
git checkout main


#git log --all (all commits of all branches)
# git log --oneline



git difftool