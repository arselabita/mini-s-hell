###### ######
# TESTCASES
###### #######

ls -la -ls -ld 			Multiple flags valid, takes the last flag cmd->argv[1]

echo hi |				Pipe at the end valid. opens PS2 for manual cmd input

cp -r -v -i src dest

